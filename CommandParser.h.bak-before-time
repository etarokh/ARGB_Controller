#pragma once

#include <Arduino.h>

#include "EffectManager.h"

class CommandParser
{
public:
  void begin(EffectManager* effectManager);
  void update();

private:
  static constexpr size_t BUFFER_SIZE = 128;

  EffectManager* effects = nullptr;

  char inputBuffer[BUFFER_SIZE];
  size_t inputLength = 0;
  bool bufferOverflow = false;

  void processLine(char* line);

  bool parseState(
    const char* value,
    MonitorState& state
  ) const;

  void printError(
    const char* message
  ) const;
};
